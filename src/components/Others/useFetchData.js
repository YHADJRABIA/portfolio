/* API data fetching hook */

/* import React from "react"; */

/* Params names to be checked on API documentation */
/* export default function useFetchData(params, page) {
  return (
      data = [1],
      loading=false,
      error =false,
      {loading && <h1>Loading...</h1>}
      {error && <h1>Error...</h1>}
      {<h1>data.length</h1>}

  );
}
 */
